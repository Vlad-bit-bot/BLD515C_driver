Still in early development, the purpose of this driver is to allow the usage of the BLD515C bldc motor controller in ros2. 
It is build on the minimalmodbus library. 
Soon will feature CANBUS support.

HOW TO USE:

Once installed, build, then source and then run:

ros2 run BLD515C_driver node_rs485
